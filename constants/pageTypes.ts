export enum PagePath {
  HOME = '/',
  TECH = '/tech',
  NOTE = '/note',
  PROJECTS = '/projects',
  SEARCH = '/search',
  INFO = '/info',
}

export enum BlogType {
  TECH = 'tech',
  NOTE = 'note',
  PROJECTS = 'projects',
  NONE = '',
}

export enum PageType {
  BLOG = 'blog',
  INFO = 'info',
  NONE = '',
}
