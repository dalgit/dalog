export enum PagePath {
  HOME = '/',
  TECH = '/tech',
  NOTE = '/note',
  PROJECTS = '/projects',
  INFO = '/info',
}

export enum BlogType {
  TECH = 'tech',
  NOTE = 'note',
  PROJECTS = 'projects',
  NONE = '',
}

export enum PageType {
  BLOG = 'blog',
  INFO = 'info',
  NONE = '',
}
